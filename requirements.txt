# MoDeRo — предиктивная аналитика состояния крутильного вала
# Версии зафиксированы под окружение разработки (Python 3.12) для воспроизводимости.
# Этап 1 (ETL) и этап 3 (признаки) требуют только pandas/numpy/openpyxl;
# остальное нужно для слоёв 1–3 и отчёта.

pandas==2.2.3
numpy==2.1.3
openpyxl==3.1.5          # чтение исходных .xlsx
scikit-learn==1.8.0      # baseline-регрессия, метрики, разбиения
catboost==1.2.10         # основная модель слоя 1 (градиентный бустинг)
lightgbm>=4.0            # альтернативный бустинг (опционально; в окружении может отсутствовать)
statsmodels==0.14.6      # автокорреляция, диагностика рядов (EDA)
scipy==1.17.1            # вспомогательные расчёты (CUSUM, статистика)
matplotlib==3.10.8       # рисунки для ПЗ
pyarrow==24.0.0          # запись таблицы признаков в parquet
jupyter==1.*             # notebooks/01_eda.ipynb, 99_report_figures.ipynb
